return {
    -- "tfpickard/openai_cost",
    -- dir = "~/src/openai-cost.nvim",
    -- config = function()
    --   require("openai_cost").setup({
    --     model = "gpt-4-turbo", -- or your preferred model
    --     cli = "openai", -- path to the OpenAI CLI
    --   })
    -- end,
}
